# 版本日志

## 1.8.23

《标点符号用法》改为跳转国家标准公开网页地址。
- 删除本地 PDF 文件及 `pdf-viewer.html` 预览页。
- 不再使用本地 PDF 下载/内嵌预览，避免浏览器下载接管工具的干扰。

## 1.8.20

围绕「金额指南」内容与演示样式做迭代。
- 重新生成「金额」规则内容：金额单位使用区分、金额单位省略说明、账单列表省略 ¥、万元写法、价格范围、小数位数、正负号、千分位分隔符。
- 保留第一点下的点式/短句式/段落式对比表格，并补充「点式文本使用 ¥，句子中使用 元」说明。
- 使用新版示意图替换账单列表省略 ¥ 的示例，图片宽度固定 400px，居中展示；示例区域去掉边框和圆角，背景色调整为 242,242,238。
- 调整打包时的静态资源内联策略，确保演示图片随单文件发布正常显示。

## 1.8.19

补充指南「命名策略」内容。
- 新增「用词专业严谨」条目，强调使用行业通用的专业表达。
- 「特定情境下使用概念包装」调整为「适时使用概念包装」。
- 补充「避免负面联想与歧义」案例，覆盖谐音、近似字形和错误联想。

## 1.8.18

新增「直接作为中文词条，AI 仅生成英文」开关。
- 「补充场景或功能描述」表单下方新增开关，默认关闭。
- 开启后，输入内容直接作为中文词条展示，AI 仅生成英文。
- 历史记录同步保存并恢复该开关状态。
- 「直接作为中文词条」和「参考词条」的开关统一放在文字前面。
- 页面所有滚动条样式统一为细滚动条，并隐藏上下箭头按钮。
- 输入框字数统计改为不加粗。
- 生成结果移除 meta-chip 标签。

## 1.8.17

优化输入区与表单样式。
- 「补充场景或功能描述」「参考词条」输入框增加字数统计和清空按钮。
- 字数统计显示在输入框内部右下角，清空按钮放在输入框外部右上角。
- 补充场景、语气风格、参考词条等标签字号统一放大。
- 语气风格增加必填星号，并修复标签关联导致标题区域也可点击下拉框的问题。
- 小号 switch 圆点位置修正为垂直居中。
- 参考词条开关关闭时隐藏清空按钮。

## 1.8.16

本地历史记录支持搜索、删除单条和清空。
- 历史记录区域新增搜索框，可按提示词类型、语气、场景、参考词条、中文/英文结果、原始返回内容等字段过滤。
- 每条历史记录新增单个删除按钮，hover 或键盘聚焦时显示，只删除选中记录。
- 有搜索词时右上角按钮切换为清除搜索，原「清空历史」功能保留。
- 搜索无结果时显示空状态提示。

## 1.8.15

生成结果支持一键复制完整双语卡片。
- 结果面板新增「复制完整卡片」按钮。
- 普通结果仅复制中文提示词和 English prompt，不再附带类型、组件等额外字段。
- 原始结果标签下复制原始返回内容。

## 1.8.14

数据表支持更多格式上传。
- 上传入口新增 `.xlsx`、`.xls`、`.json`、`.md`、`.markdown` 格式支持。
- JSON 支持常见字段名：`source/target`、`zh/en`、`中文/英文`、`原文/译文`、`key/value` 等。
- Markdown 支持管道表格，自动识别表头与分隔行。
- Excel 自动读取第一个工作表，支持无表头的两列词条数据。
- 旧版本已上传的 CSV/TSV/TXT 数据仍然兼容。
- 新增 UTF-8 BOM 清理，避免带 BOM 的文件解析失败。

## 1.8.13

调整现有词条的相似度计算规则。
- 相似度改为 `输入长度 / 目标长度`，四舍五入到个位百分比。
- 例如输入“保存”（长度 2）命中“保存成功”（长度 4），分数为 50%。
- 命中“请先保存文档”（长度 6），分数为 33%。
- 移除原先 50% 以下不展示的过滤规则，子字符串命中的结果全部保留并按分数从高到低展示。
- 新增可复用发布打包脚本，统一完成构建、内联资源、HTML 校验和 zip 打包。

## 1.8.12

补充场景/功能描述输入框改为必填。
- 「补充场景或功能描述」标为必填项，未填写时点击「生成双语提示词」或「查询现有词条」会显示引导提示。
- 填写场景内容后或再次点击操作按钮时，必填提示会自动关闭。
- 修复发布包内联脚本解析问题，避免打开 `index.html` 时浏览器显示源码。

## 1.8.11

增加未配置模型时的生成拦截提示。
- 未配置有效 AI 模型时，点击「生成双语提示词」不会直接请求模型。
- 提示条提供「配置模型」按钮，可快捷打开模型设置窗口。
- 用户也可选择「暂不配置」关闭提示。

## 1.8.10

修复窄屏下的展示与交互问题。
- 现有词条结果不再截断省略，长内容会自动换行完整展示。
- 修复最窄宽度下「模型状态/模型设置」被下方提示词类型卡片覆盖的问题，头部会按内容自然撑高。
- 窄屏下的模型状态和模型设置按钮会纵向堆叠，避免位置错乱。

## 1.8.9

小版本体验调整。
- 「功能介绍」默认全选短句、长句、段落，切回该类型时也会自动恢复全选。
- 顶部「当前组合」文案调整为用户更直观的「当前类型」。

## 1.8.8

修复模型设置弹窗的输入框样式一致性问题。
- API Base URL、模型名称输入框显式声明为 `type="text"`，确保样式选择器可以稳定匹配。
- API Key 输入框保留图标预留位，避免图标与密码文字重叠。
- HTML 声明 `lang="zh-CN"`，并增加 `Content-Type: text/html; charset=UTF-8`，降低其他电脑解压后打开乱码的风险。
- 修复发布包 `index.html` 内联脚本的占位替换残留，重新生成可直接双击打开的单页产物，避免浏览器把 JS 源码当文本展示。

## 1.8.7

现有词条检索改为子字符串匹配。

- 检索不再要求完全一致，只要词条内容或标题中包含输入关键词即可命中。
- 例如输入「保存」，会同时匹配「请先保存文档」「保存成功」等包含该关键词的词条。
- 检索结果不再只保留 10 条，相似度 50% 以上的结果全部保留。
- 匹配结果按相似度从高到低排序，每页展示 10 条并支持分页浏览。
- 同步更新现有词条空状态说明文案。

## 1.8.6

新增「继续追问」能力。

- 生成结果后可在「重新生成」旁进行继续追问。
- 追问时可输入调整建议，AI 会结合当前生成结果重新生成完整结果。
- 追问输入区支持取消与空内容禁用发送。

## 1.8.5

初步成功的版本。

- 顶部展示当前版本号。
- 移除模拟生成功能，生成结果改为直接调用已配置模型。
- 数据表名称超长时自动截断，鼠标悬停显示完整名称。
- 修正提示词类型面板底部间距。
- 优化语气风格下拉的浅色解释文字展示。

## 1.8.2

「生成词条」和「现有词条」改为标签页展示。
- 用户点击「生成双语提示词」后自动切换到生成结果。
- 用户点击「查询现有词条」后自动切换到现有词条。
- 两个面板会保留当前状态，切换标签不会丢失已生成结果或已验证词条。

## 1.8.1

优化内置词条加载与本地查询，避免运行时请求 `.csv` 文件触发下载工具误拦。
- 内置 TM 词条改为打包进应用并按需加载，不再通过 `fetch('/tm/*.csv')` 请求文件。
- 本地检索加入词条规范化缓存，重复查询无需反复重新计算相似度。
- 内置词条按表拆分为独立资源，主页面首屏加载更轻。

## 1.8.0

查询现有词条与生成双语提示词拆分为两个独立入口。
- 输入提示词信息后，可先点击「查询现有词条」检索 TM 数据表。
- 查询不再依赖 AI 生成结果，可单独使用。
- 保留「生成双语提示词」入口，按需生成提示词。
- 现有词条模块展示中英文词条与来源数据表。

## 1.7.0

新增“现有词条”模块，支持 TM 数据检索与管理。

- 每次 AI 生成后自动检索已启用的数据表，最多展示 10 条相近词条。
- 没有相近词条时显示“产品中无相关词条”。
- 默认加载 TM 文件夹中的三个 CSV 数据表，并全部启用。
- 支持上传 CSV/TSV/TXT 词条数据表。
- 支持删除数据表、启用/停用数据表。
- 停用状态下不会参与词条检索。
- 上传的数据表会保存在本地浏览器中。

## 1.6.1

修复顶部 AI 状态不随配置更新的问题。

- 启动时根据本地保存的模型配置初始化顶部连接状态。
- 测试连接成功或失败后，顶部状态会同步变化。

## 1.6.0

历史记录支持完整恢复生成配置。

- 选择历史记录时，会同步恢复补充场景/功能描述。
- 如果有参考词条，会自动恢复参考词条开关与内容。
- 有输出长度配置的类型，会恢复历史选择的具体长度。
- 历史记录结构扩展保存场景、参考词条和输出长度配置。

## 1.5.2

优化 AI 原始输出结构。

- 未选择输出长度时，提示 AI 不要返回 `results` 和 `mode` 字段。
- 仅在选择输出长度后才要求按长度返回多组结果。
- Demo 模式的原始结果不再包含空 `results` 数组。

## 1.5.1

修复结果标签沿用旧结果输出长度的问题。

- 结果展示严格跟随当前提示词类型的长度配置。
- 切换提示词类型时会清空当前结果，避免误用上一个类型的结果标签。

## 1.5.0

结果展示跟随提示词类型的输出长度要求。

- 有输出长度要求的类型，按当前选中的长度展示对应结果，再加“原始结果”。
- 没有输出长度要求的类型，展示“结果”和“原始结果”。
- 展示类型由当前提示词类型的长度配置驱动，不完全依赖模型返回结构。

## 1.4.0

结果面板的“其他”调整为“原始结果”。

- 展示 AI 返回的原始 JSON 或模型输出内容，方便用户核对数据结构。
- 每个类型的生成结果都配备“原始结果”查看入口。
- Demo 模式会生成可读的模拟原始结果，便于预览。
- 原始结果支持一键复制。

## 1.3.0

生成结果面板支持多长度结果与额外内容展示。

- 支持同时展示短句、长句、段落三种输出结果，并通过标签页切换。
- 新增“其他”区域，展示 AI 生成的补充建议、备选文案或说明。
- AI 返回结构扩展为多结果 JSON，Demo 模式同步支持多结果展示。
- 结果面板提供分长度内容的独立复制能力。

## 1.2.0

功能介绍输出长度升级为多选。

- 短句、长句、段落三种长度支持同时勾选。
- 默认全选三种长度，可按需求自由切换。
- AI 生成提示会带入当前选中的所有输出长度。
- 输出长度选择器改为多选胶囊样式，提升交互识别度。

## 1.1.0

支持基于 tips-creator references 的完整提示词创作规则。

- 为全部提示词类型补充目的、写作规则和参考示例。
- 为全部语气风格补充适用场景、写作要求和示例。
- 首页新增“提示词要点”参考面板，展示当前类型和语气的创作依据。
- 功能介绍支持短句、长句、段落三种输出长度。
- AI 生成提示中纳入类型示例、语气规则和语气示例。
- 移动端适配参考面板与输出长度选择。

## 1.0.0

基础功能版本。

- 双语产品提示词工作台。
- 支持 12 类提示词与 6 种语气。
- 支持 OpenAI 兼容接口配置，用户自带 API Key。
- 支持未配置 Key 时的模拟生成模式。
- 中英文结果展示、复制、重新生成。
- 本地历史记录与本地配置保存。
